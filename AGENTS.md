# 디자인 레퍼런스 아카이브 작업지시서

## 목적

이 폴더는 웹·앱 콘텐츠 디자인, 브랜드 디자인, AI 디자인, 비주얼 그래픽 및 기타 디자인 영감을 수집하고 다시 찾기 쉽게 정리하는 개인 아카이브다. 자료는 15년차 UI 디자이너의 관점에서, 완성도·새로움·실무 적용 가능성이 높은 것을 우선한다.

이 아카이브의 수집 단위는 정보가 아니라 **재현·재사용 가능한 시각 표본(스펙시먼)**이다. 유효한 엔트리는 최소한 시각 아티팩트, 시각 속성 태그(시각 렌즈), 원본 링크와 짧은 주석을 갖춘다. 이 조건을 만족하지 못하는 순수 텍스트·설명형 자료는 카드로 수집하지 않는다.

## 최종적으로 보여 주는 방식

사용자가 보는 결과물은 표나 긴 텍스트가 아니라 **이미지 미리보기와 원본 링크가 중심인 레퍼런스 카드형 아카이브**여야 한다.

카드 화면·스타일·새 컴포넌트를 수정할 때는 루트의 DESIGN.md를 먼저 읽고, 그 안의 토큰·레이아웃·컴포넌트 규칙을 따른다.

각 레퍼런스 카드에는 아래 정보를 포함한다.

- 분야는 `AI 디자인`, `웹 스타일가이드`, `콘텐츠 디자인 가이드`, `Figma 플러그인`, `AI 이미지 프롬프트`, `AI 영상 프롬프트`, `웹·앱 UI`, `비주얼 그래픽`, `브랜드`, `공감형 웹툰` 중 하나로 지정한다.

- 대표 이미지, 스크린샷 또는 결과물(필수). 원본의 공개 대표 이미지·스크린샷·결과 이미지를 확인할 수 없으면 카드를 생성하지 않고 수집을 보류한다. `Figma 플러그인`은 플러그인 실행 화면 또는 공식 커버 이미지를 사용한다.
- 제목 또는 짧은 설명
- 원본 링크
- 분야
- 스타일/핵심 특징 1~2개
- 검색용 태그
- 수집일 (`YYYY-MM-DD`)
- 왜 참고할 만한지에 대한 짧은 디자이너 메모

이미지를 안전하게 저장할 수 없거나 저작권·접근 제약이 있는 경우에는 이미지를 무단 복제하지 말고, 링크와 썸네일/이미지 확인 위치를 제공한다. 대표 시각물을 확인조차 할 수 없는 경우에는 카드를 생성하지 않는다.

모든 신규 카드는 원본 링크가 제공하는 `og:image`, 공식 대표 커버 또는 공개 미리보기 URL을 확인해 `thumbnail`로 기록한다. 추상 도형만으로 대표 시각물을 대체하는 카드는 `시각 게이트 미충족`으로 보고 수집하지 않는다. 원격 이미지는 카드가 URL을 직접 표시하고 로컬이나 GitHub에 복제·저장하지 않으며 원본 링크와 출처를 유지한다.

## 소스 우선순위 (allowlist)

수집은 아래 비주얼 우선 소스를 먼저 탐색한다. 하루 수집분에서 비주얼 우선 소스 비중을 60% 이상으로 유지하며, 일반 블로그·아티클은 실제 화면·컴포넌트·결과물이 확인될 때만 사용한다.

- `비주얼 그래픽`, `브랜드`, `웹·앱 UI`, `AI 디자인`: Behance, Dribbble, Cosmos, Savee, Are.na, SiteInspire, Landbook, Godly
- `AI 이미지 프롬프트`: Lexica, PromptHero, Midjourney 공개 갤러리, Civitai 공개 이미지
- `AI 영상 프롬프트`: Runway·Pika·Kling 공식 쇼케이스, 공개 데모 릴
- `웹 스타일가이드`: 실제 기업·브랜드가 운영하는 공개 디자인 시스템 사이트 중 화면·컴포넌트 상태가 보이는 것
- `콘텐츠 디자인 가이드`: 실제 브랜드의 공개 콘텐츠·라이팅 가이드 중 화면·전후 비교·실제 카피가 보이는 것
- `Figma 플러그인`: Figma Community, 공식 플러그인 페이지 중 실행 화면·커버가 확인되는 것
- `공감형 웹툰`: Instagram 공개 웹툰 캐러셀, 작가 공식 계정, 브랜드 협업 공감툰 중 컷 구성·말풍선·자막·반응 유도가 실제 이미지로 확인되는 것

위 목록은 기본값이다. 텍스트만 반환하는 소스가 반복 유입되면 우선순위를 낮추거나 목록에서 제외한다.

## 수집 기준

- 매일 오전 6시 17분 수집을 기준으로 하며, 컴퓨터가 꺼져 있어 실행을 놓친 경우 다시 켠 뒤 최대 30분 안에 오늘자 누락 수집을 보충한다.
- 보충 실행은 오늘 날짜로 이미 수집된 카테고리를 먼저 확인하고 빠진 분야만 추가한다. 10개 분야가 모두 있으면 파일을 변경하지 않는다.
- 기본적으로 각 카테고리에서 1개씩 수집하되, `AI 이미지 프롬프트`와 `AI 영상 프롬프트`는 각각 2개씩 수집한다. 여기에 자동차 관련 `AI 이미지 프롬프트`를 매일 3개 더 수집해 하루 최대 15개의 새 레퍼런스를 추가한다. 다른 카테고리의 항목으로 빈 분야를 대체하지 않는다.
- 시각 게이트를 통과한 후보가 없는 분야는 억지로 채우지 않고 빈 채로 두며 사유를 보고한다.
- 기본 조사 범위는 최근 30일이며 적합한 자료가 없으면 최근 90일까지 확대한다. 그래도 기준을 충족하지 못하면 억지로 추가하지 않고 해당 분야와 사유를 보고한다.
- 최신성과 디자인 품질을 함께 고려해 큐레이션한다.
- 매일 수집 주제에 테크·IT 분야를 포함한다. AI 제품, 개발자 도구, 클라우드·데이터, 사이버보안, 반도체·하드웨어, 미래형 인터페이스처럼 기술적 맥락이 실제 화면·브랜드·그래픽 결과물에서 확인되는 사례를 우선하고, 기술 뉴스나 설명 텍스트만 있는 자료는 카드로 만들지 않는다.
- 단순히 유행하는 결과물보다 정보 구조, 사용자 흐름, 타이포그래피, 브랜드 일관성, 접근성, 인터랙션, AI 활용 방식 등 실무적으로 배울 점이 있는 자료를 우선한다. 동시에 스타일·컬러·구도·질감·무드가 명확한 시각 표본을 우선한다.
- 웹 스타일가이드와 콘텐츠 디자인 가이드는 설명 문서만 수집하지 않는다. 실제 기업·브랜드가 운영하는 디자인 시스템과 콘텐츠 가이드를 우선하고, 원칙이 실제 웹 화면, 컴포넌트 상태, 전후 비교, 실제 카피 또는 사용 흐름에서 보이는 사례만 수집한다.
- 가이드 항목은 원본의 공개 미리보기 이미지 또는 실제 화면이 확인될 때만 추가한다. 이미지 없이 텍스트만 있는 가이드는 보조 링크로만 활용하고 카드로 수집하지 않는다.
- `공감형 웹툰`은 캐러셀 전체 흐름 또는 대표 컷이 공개돼 있어야 하며, 공감 포인트가 카피·표정·컷 전개에서 실제로 확인될 때만 수집한다. 단순 텍스트 밈, 스레드 캡처, 반응형 문장만 있는 포스트는 카드로 수집하지 않는다.
- AI 프롬프트는 특정 작가·스튜디오·브랜드의 고유 스타일을 모사하지 않는다. 색, 구도, 소재, 조명, 질감, 카메라와 움직임처럼 일반화 가능한 시각 요소로 작성한다.
- AI 이미지 프롬프트는 사진형 결과에 편중하지 않고, 광고 캠페인에 적용 가능한 높은 완성도의 3D 제품 이미지·CGI·재질 및 라이팅 실험도 적극 수집한다. 자동차처럼 제품 형태와 표면 표현이 중요한 주제에서는 실제 광고 수준의 조명, 카메라, 배경, 재질 제어가 확인되는 결과를 우선한다.
- 매일 추가하는 자동차 AI 이미지 프롬프트 3개는 서로 다른 스타일이어야 한다. 예: 스튜디오 제품 광고, 시네마틱 로케이션, 추상 3D·모션 그래픽, 에디토리얼 포스터, 미래적 콘셉트 비주얼. 같은 차종·구도·조명·배경의 미세 변주는 중복으로 제외한다.
- 자동차 AI 이미지 프롬프트는 차체 비례와 디테일, 도장·금속·유리·타이어 재질, 반사 환경, 키·림·필라이트, 카메라·렌즈, 배경과 색보정이 광고 그래픽 수준으로 통제되고 결과물에서 확인될 때만 `accept`한다.
- 매일 수집하는 AI 이미지 프롬프트 중 최소 1개는 블루·시안·네이비 계열의 컬러 처리가 결과물에서 명확히 확인되는 후보를 우선 탐색한다. 단순히 프롬프트에 blue가 포함된 것만으로 채우지 않고, 색 대비·광원·재질·무드가 광고 그래픽 수준으로 통제된 결과만 `accept`한다. 적합한 후보가 없으면 다른 색의 강한 결과물로 대체하지 말고 미수집 사유를 보고한다.
- AI 프롬프트 유형은 결과 이미지 또는 영상(output)이 함께 확인될 때만 수집한다. 프롬프트 텍스트만 있으면 수집하지 않는다.
- 중복을 제거하고 10개 필수 카테고리가 고르게 쌓이도록 관리한다.
- 출처와 원본 링크를 가능한 한 반드시 남긴다. 출처를 확인할 수 없으면 신뢰도 낮음으로 표시하거나 제외한다.

## 렌즈 정의

자동 수집에서 활성 렌즈 목록이 입력에 명시되지 않으면 아래 고정 렌즈를 사용한다.

- 정보 렌즈: `정보구조`, `사용자흐름`, `타이포그래피`, `브랜드일관성`, `접근성`, `인터랙션`, `AI활용`, `콘텐츠명료성`
- 시각 렌즈: `스타일`, `컬러처리`, `구도·레이아웃`, `타이포처리`, `질감·광원`, `무드·톤`, `모션필`

후보가 기여하는 정보·시각 렌즈를 각각 기록한다. 시각 중심 분야인 `비주얼 그래픽`, `브랜드`, `AI 이미지 프롬프트`, `AI 영상 프롬프트`, `웹·앱 UI`, `AI 디자인`, `공감형 웹툰`은 시각 렌즈를 최소 1개 기록한다. `웹·앱 UI`는 정보 렌즈와 시각 렌즈를 함께 기록한다.

시각 렌즈 값은 아래 닫힌 어휘를 사용하고 새 값은 주간 검수 때만 추가한다.

- `스타일`: 에디토리얼, 브루탈리즘, 미니멀, 클레이모피즘, 글래스모피즘, Y2K, 스위스, 뉴트로, 3D, 일러스트
- `컬러처리`: 파스텔, 하이채도, 모노톤, 듀오톤, 그라디언트, 어스톤
- `구도·레이아웃`: 그리드, 비대칭, 중앙집중, 고밀도, 여백중심
- `질감·광원`: 소프트확산광, 하드라이트, 필름그레인, 매트, 글로시
- `무드·톤`: 따뜻, 차가움, 유희적, 절제, 럭셔리, 몽환

## 수집 심사 게이트

모든 후보는 이미 주어진 분야를 그대로 사용하며 다시 분류하지 않는다. 분야에 대응하는 가치축은 다음 `value_axis.map`을 따른다.

- 웹 스타일가이드, 웹·앱 UI: `시스템화`
- 콘텐츠 디자인 가이드: `실행가능성`
- 비주얼 그래픽, 브랜드, 공감형 웹툰: `스타일추출성`
- AI 이미지 프롬프트, AI 영상 프롬프트: `재현성`
- Figma 플러그인: `유용성`
- AI 디자인: 임시로 `재현성`

각 후보는 공통축 `완성도`, `신규성`, `근거성`과 분야별 가치축 하나를 각각 1~5점으로 채점한다.

- 완성도: 1=러프, 3=정돈됐으나 부족, 5=프로덕션·발행급
- 신규성: 1=흔함, 3=기존 접근의 변주, 5=없던 새 접근
- 근거성: 1=취향 외 설명 불가, 3=일반론 수준, 5=구체적이고 검증 가능
- 시스템화: 1=규칙화 불가, 3=부분 패턴화, 5=토큰·컴포넌트로 흡수 가능
- 스타일추출성: 1=방향 없음, 3=일부 참고, 5=재사용 가능한 무드·스타일 추출 가능
- 재현성: 1=프롬프트 없음, 3=프롬프트만 있음, 5=프롬프트·모델·설정·결과물(output)이 모두 있음
- 실행가능성: 1=추상론, 3=참고 가능, 5=즉시 실행 가능한 절차
- 유용성: 1=실무와 무관한 장난감, 3=가끔 사용, 5=반복 실무 시간을 확실히 절감

판정 순서는 다음과 같다.

0. 시각 아티팩트(대표 이미지·스크린샷·결과물)가 확인되지 않으면 전 분야 공통으로 `quarantine`.
1. 근거성 2점 이하이면 `quarantine`.
2. 시각 중심 분야인데 시각 렌즈 배열이 비어 있으면 `quarantine`.
3. 정보·시각 기여 렌즈가 모두 비어 있으면 `quarantine`.
4. 네 축 합계가 15점 이상이고 근거성이 3점 이상이면 `accept`.
5. 그 외는 `quarantine`.

`accept` 후보만 아카이브 카드로 추가한다. 근거 문장은 무엇이 왜 어느 업무에 유용하거나 전이 가능한지 한 문장으로 구체적으로 작성한다. 일반론만 가능한 후보는 근거성 1~2점으로 처리한다. 경쟁사 미공개 자료 또는 특정 브랜드에 종속된 자료는 `abstract_only`, 그 외는 `raw_ok`로 다룬다. AI 유형은 프롬프트·모델·설정·결과물(output)이 모두 없으면 재현성 5점을 줄 수 없으며, 필요한 정보가 부족하면 `quarantine`한다.

모든 후보의 심사 결과는 아래 고정 JSON 스키마를 사용한다. 키 이름을 바꾸거나 유형을 다시 분류하지 않는다. 점수는 1~5의 정수이며 `total`은 네 점수의 합이다. `type_block`에는 해당 유형에 필요한 메타만 넣고, AI 유형은 프롬프트·모델·설정·결과물·`fragment_map`을 반드시 포함한다.

```json
{
  "유형": "(입력값 그대로)",
  "가치축_이름": "시스템화 | 스타일추출성 | 재현성 | 실행가능성 | 유용성",
  "scores": { "완성도": 0, "신규성": 0, "근거성": 0, "가치축": 0 },
  "total": 0,
  "verdict": "accept | quarantine",
  "근거": "한 문장",
  "metadata": {
    "정보렌즈": [],
    "시각렌즈": [],
    "시각아티팩트_url": "",
    "커버리지_카테고리": "",
    "보안_등급": "raw_ok | abstract_only",
    "type_block": {}
  },
  "출처": { "소스": "", "수집일": "YYYY-MM-DD", "수집사유": "" }
}
```

AI 이미지·영상 프롬프트 유형의 `type_block`은 아래 형태를 따른다. `fragment_map`은 프롬프트 조각과 시각 효과를 매핑해 조합 가능한 통제 어휘로 만든다.

```json
{
  "prompt": "전체 프롬프트 텍스트",
  "tool_model": "예: Midjourney v7 / Runway Gen-3",
  "params": "ar, stylize, seed 등",
  "output_url": "결과 이미지·영상 링크(필수)",
  "fragment_map": [
    { "조각": "shot on Portra 400", "효과": "따뜻한 필름 그레인" },
    { "조각": "soft window light", "효과": "부드러운 확산광" }
  ]
}
```

신규 `accept` 카드에는 위 객체를 `review` 필드로 함께 저장한다. `quarantine` 후보는 `data/references.js`에 추가하지 않는다.

## 탐색과 검색

- 아카이브는 카드형 그리드와 카테고리 필터를 기본 탐색 구조로 유지한다.
- 검색은 제목, 분야, 설명, 디자이너 메모, 태그, AI 프롬프트, 시각 렌즈 값을 통합 검색해야 한다.
- 검색어와 카테고리 필터는 동시에 적용되어야 하며 결과 수를 즉시 표시한다.
- 시각 렌즈(스타일·컬러처리·무드 등)로 필터링할 수 있게 한다.
- 검색 결과가 없을 때는 빈 화면 대신 초기화 동작이 포함된 안내 상태를 제공한다.
- 키보드 탐색, 명확한 포커스 표시, 모바일 반응형 레이아웃을 유지한다.

## 분류와 태그

- 자료는 먼저 `분야별`, 다음으로 `스타일별`(시각 렌즈)로 탐색할 수 있게 정리한다.
- 모든 항목에 수집일을 남긴다.
- 태그는 짧고 일관된 한국어 중심 키워드를 사용한다. 예: `#온보딩`, `#대시보드`, `#미니멀`, `#3D`, `#에디토리얼`, `#생성형AI`.
- 시각 렌즈 값은 태그와 별도로 구조화 필드로 유지해 필터·집계가 가능하게 한다.
- 기존 항목의 링크, 프로젝트, 시각적 결과물, 핵심 콘셉트가 실질적으로 같으면 새로 추가하지 않는다.

## 안전 및 파일 관리

- 매일 오전 6시 17분 수집과 컴퓨터 재시작 후 누락 보충이 끝나면 검증을 통과한 아카이브 변경을 GitHub `yi-bellea/design-archive` 저장소의 `main` 브랜치에 즉시 게시한다.
- 매주 일요일 오전 9시에는 한 주 수집 결과를 추가 검수하고 아직 게시되지 않은 변경이 있을 때만 GitHub에 게시한다. 새 시각 렌즈 값은 이때 닫힌 어휘에 반영할지 판단한다.
- GitHub 게시 시 `DESIGN.md`, `index.html`, `assets/`, `data/`의 아카이브 관련 변경만 스테이징한다. `AGENTS.md`, 비밀정보, 임시 파일, 무관한 사용자 변경은 자동 게시에서 제외한다.
- GitHub 게시 전 JavaScript 구문, 중복 URL, 시각 아티팩트 URL을 포함한 필수 필드, 카테고리 분포를 검사한다. 검사 실패·인증 실패·원격 충돌이 있으면 강제 푸시하거나 덮어쓰지 말고 중단 후 보고한다.
- force push, 브랜치 삭제, reset, 기존 커밋 수정은 자동화에서 금지한다.
- 원본 파일을 임의로 삭제하거나 덮어쓰지 않는다.
- 파일명과 폴더명은 한눈에 이해할 수 있게 짓고, 날짜는 `YYYY-MM-DD` 형식을 사용한다.
- 저작권이 있는 이미지·파일은 필요한 범위에서만 다루고 원 출처를 보존한다.

## 작업 완료 기준

매 수집 작업이 끝나면 다음을 확인한다.

1. 새 자료가 카드형 화면/문서에서 이미지(필수)와 링크로 바로 확인되는가. 이미지가 없는 카드는 존재해선 안 된다.
2. 분야, 스타일(시각 렌즈), 태그, 수집일, 디자이너 메모가 채워졌는가.
3. 시각 중심 분야 카드에 시각 렌즈가 1개 이상 기록됐는가. AI 프롬프트 카드에 결과물(output)과 `fragment_map`이 있는가. `공감형 웹툰` 카드에는 대표 컷 또는 캐러셀 대표 이미지와 공감 포인트 메모가 있는가.
4. 기존 아카이브와 중복되지 않는가.
5. 전체 목록 또는 인덱스에서 새 자료를 찾을 수 있는가.
6. 사용자에게 카테고리별 추가 결과, 총 추가 수, 제외한 중복 수, 시각 게이트 탈락 수와 미추가 사유를 짧게 요약했는가.
7. 새 아카이브 변경이 있으면 검증 후 GitHub `main` 게시가 성공했는가.

## 커뮤니케이션

- 설명과 결과 보고는 한국어로, 짧고 명확하게 작성한다.
- 확신하기 어려운 출처·저작권·이미지 사용 가능 여부는 추측하지 말고 표시한다.
- 시각 게이트 탈락이 많은 분야는 반복 시 소스 우선순위 조정을 제안한다.
- 매일 수집·누락 보충·일요일 검수의 GitHub 게시 외에는 자동으로 외부 업로드, 삭제, 공개하지 않는다.
